#include<stdio.h>
int main()
{
    float base,leanth,area;
    printf("Enter the base and leanth ");
    scanf("%f %f",&base,&leanth);
    area =0.5*(base*leanth);
    printf("the Triangle area :%f",area);
}
